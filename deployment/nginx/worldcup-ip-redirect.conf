# Redirect direct IP access to canonical HTTPS domain
# Copy to: /etc/nginx/sites-available/worldcup-ip-redirect

server {
    listen 80;
    listen [::]:80;
    server_name 91.107.188.229;

    return 301 https://footballpredictor.it.com$request_uri;
}
