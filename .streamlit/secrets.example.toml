# Copy to `.streamlit/secrets.toml` on Streamlit Cloud / HF Spaces.
# Never commit real secrets.

API_FOOTBALL_KEY = ""
OPENAI_API_KEY = ""

# Phase 49 — public access
PUBLIC_ACCESS_ENABLED = "true"
FREE_DAILY_PREDICTION_LIMIT = "2"
PAID_UNLOCK_PRICE_EUR = "5"
APP_PUBLIC_URL = "https://your-app.streamlit.app"

# Admin — Developer Mode (Phase 49 security)
ADMIN_USERNAME = ""
ADMIN_PASSWORD = ""

# Optional deployment gate (separate from user accounts)
APP_AUTH_ENABLED = "false"
APP_USERNAME = ""
APP_PASSWORD = ""

# Stripe (use Payment Link OR Checkout session)
STRIPE_SECRET_KEY = ""
STRIPE_PRICE_ID = ""
STRIPE_PAYMENT_LINK = ""
STRIPE_WEBHOOK_SECRET = ""
