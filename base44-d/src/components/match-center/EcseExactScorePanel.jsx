import EndResultCandidatesPanel from "./EndResultCandidatesPanel";

/** @deprecated Use EndResultCandidatesPanel — kept for existing imports. */
export default function EcseExactScorePanel(props) {
  return <EndResultCandidatesPanel {...props} />;
}
